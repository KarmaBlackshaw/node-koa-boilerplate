/**
Usage:

const redis = require('@config/redis')

redis.socketEmitUser('{{pluralKebabCase name}}', 'sample-event-name', 'sample-data')

redis.socketEmitAdmin('{{pluralKebabCase name}}', 'sample-event-name', 'sample-data')

 */
module.exports = io => {
  io.on('connection', () => {
    console.log('{{pluralKebabCase name}} socket connected')
  })

  return io
}
